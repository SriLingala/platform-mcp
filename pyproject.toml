[project]
name = "platform-mcp"
version = "0.1.0"
description = "An MCP server that exposes platform engineering knowledge as Claude-callable tools."
authors = [{ name = "Sri Lingala" }]
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
